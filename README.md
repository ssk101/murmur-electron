Python > 3 required

Mac:
$ brew install python
$ pip3 install hidapi
$ yarn start

Linux:
$ sudo ./udev-rules.sh # This adds permissions to interact with the Luxafor USB device.
$ pip install pyusb
$ yarn start
